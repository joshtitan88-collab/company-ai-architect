# Canned voice notes

Voice rendering must use deployment-managed credentials and must never record
credential metadata, personal filesystem paths, or environment inspection in
this public repository. Generated clips should be verified for consistent
voice identity before deployment.
